/*

*/
#include<stdio.h>
void start(int count[],int N);
int main() {
	int N;
	scanf("%d",&N);
	int count[N];
	for(int i=0; i<N; i++) {
		scanf("%d",&count[i]);
	}
	start(count,N);

	return 0;
}
void start(int count[],int N) {
	int start,end;
	int sum = 0;
	
}
